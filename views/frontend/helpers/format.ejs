<%
    formatDateHelper = (time) => {
        return moment(time).format(systemConfigs.time_format);
    }

    formatSummaryHelper = (content) => {
        return content.replace(/<[^>]*>/igm, "").substring(0, 150);
    }
%>