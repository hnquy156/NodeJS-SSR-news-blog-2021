<% 
    checkboxHelper = (index, id) => {
        
        return `<div class="custom-control custom-checkbox">
                    <input class="custom-control-input" type="checkbox" id="checkbox-${index}" name="cid" value="${id}">
                    <label for="checkbox-${index}" class="custom-control-label"></label>
                </div>`
    }
%>
